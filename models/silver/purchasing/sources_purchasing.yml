version: 2

sources:
  - name: source_dw
    description: Sources from DW with dataset od AdventureWorks
    schema: dev_thaina
    tables:        
      - name: shipmethod
        description: Shipping company lookup table.
        columns:
          - name: shipmethodid
            tests:
              - not_null
              - unique

